<Page xmlns="http://schemas.nativescript.org/tns.xsd"
      navigatingTo="navigatingTo" class="page" actionBarHidden="true"
      xmlns:df="nativescript-ui-dataform">

        <StackLayout class="p-20">
            <Label text="{{L('recover_password')}}" class="h1 text-center" textWrap="true"/>
            <df:RadDataForm id="recover-pass-form" source="{{source_object}}" metadata="{{metadata}}"
            ></df:RadDataForm>
            <Button tap="onRecoverBtn" text="{{L('recover_password')}}"></Button>
        </StackLayout>
</Page>